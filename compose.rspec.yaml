services:
  app:
    environment:
      - RAILS_ENV=test
      - CI=true
    build:
      context: .
      dockerfile: ./Dockerfile.prod
    image: "tkycraft/mahirun:rspec1.0"
    depends_on: {}
    volumes: []
    healthcheck: {}
    entrypoint: []
